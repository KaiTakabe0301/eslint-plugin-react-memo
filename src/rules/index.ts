import requireUseCallbackInHooks from './require-usecallback';
import requireUseMemo from './require-usememo';

export const rules = {
  'require-usecallback': requireUseCallbackInHooks,
  'require-usememo': requireUseMemo,
};
