import { configs } from './legacy-config';
import { flatConfig } from './flat-config';
import { rules } from './rules';

export { configs } from './legacy-config';
export { flatConfig } from './flat-config';
export { rules } from './rules';

export default {
  rules,
  configs,
  flatConfig,
};
